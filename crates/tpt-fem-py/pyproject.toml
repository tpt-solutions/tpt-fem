[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "tpt_fem"
requires-python = ">=3.8"
description = "Python bindings for the tpt-fem finite-element core."
license = { text = "MIT OR Apache-2.0" }
